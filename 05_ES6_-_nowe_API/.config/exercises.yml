exercises:
    '01_Array API': { max_points: 0, disabled: true, grading: [{ points: 2, criterion: 'Poprawna Rysowanie miecza za pomocą wyznaczonych metod' }] }
    '02_String - metody': { max_points: 0, disabled: true, grading: [{ points: 2, criterion: 'Poprawna Rysowanie miecza za pomocą wyznaczonych metod' }] }
    '03_Destrukturyzacja - rest i spread': { max_points: 0, disabled: true, grading: [{ points: 0.25, criterion: 'Poprawna otrzymanie danych z `guardedTreasure` za pomocą destrukturyzacji' }, { points: 0.25, criterion: 'Pobranie samorodków i dodanie ich do zmiennych' }, { points: 0.25, criterion: 'umieszczenie samorodków w zmiennej `scavengerPocket` za pomocą operatora `spread`' }] }
    04_Funkcje: { max_points: 0, disabled: true, grading: [{ points: 0.25, criterion: 'Uspójnienie nazw zmiennych' }, { points: 0.25, criterion: 'Zastąpienie funkcji callback funkcjami strzałkowymi i usunięcie zmiennych konteksotowych (`that`, `_this`, `context`)' }, { points: 0.25, criterion: 'Usunięcie deklaracji za pomocą var' }] }
    '05_Mapy i sety': { max_points: 0, disabled: true, grading: [{ points: 0.25, criterion: 'Implementacja funkcji `hasBook(category, title)`' }, { points: 0.25, criterion: 'Implementacja funkcji `putBook(category, title)`' }, { points: 0.25, criterion: 'Implementacja funkcji `removeBook(title)`' }] }
    '06_Gettery i Settery': { max_points: 0, disabled: true, grading: [{ points: 0.25, criterion: 'Dodanie do obiektu zmiennej `counterVal`' }, { points: 0.25, criterion: 'Implementacja metody `get count()`' }, { points: 0.25, criterion: 'Implementacja metody `set count(val)`' }] }
